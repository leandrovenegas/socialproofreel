## CAPABILITIES (Vercel Dashboard)
- **Template Control:** JSON-based UI to modify aesthetic variables (colors, fonts)[cite: 1].
- **Visual Preview:** CSS-based simulation of the final video layout (mock-up)[cite: 1].
- **Lead Management:** CRUD operations for the 6,000 leads database[cite: 1].
- **Multi-tenancy:** Capability to manage different business accounts using the Next.js starter[cite: 1].